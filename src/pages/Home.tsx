import Balatro from "@/components/Balatro";

const Home = () => {
    return (
      <div className="fixed inset-0 w-full h-full min-h-screen">
        {/* <Balatro /> */}
      </div>
    );
  };

export default Home;